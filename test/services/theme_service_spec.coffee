describe 'Service: Theme', ->

  beforeEach(module('ThemeEditor'))

  xit 'processes a valid theme', inject (Theme) ->
  xit 'bg color', inject (Theme) ->
  xit 'fg color', inject (Theme) ->
  xit 'selection color', inject (Theme) ->
  xit 'gutter fg color', inject (Theme) ->
  xit 'color box border color', inject (Theme) ->
  xit 'valid css for scopes', inject (Theme) ->
  xit 'valid css for gutter', inject (Theme) ->
  xit 'valid css for selection', inject (Theme) ->
  xit 'font styles', inject (Theme) ->
